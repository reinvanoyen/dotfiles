# ~/.huskyrc
# This loads nvm.sh and sets the correct PATH before running hook
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
[ -f "$PWD/.nvmrc" ] && nvm use >/dev/null 2>&1 || true